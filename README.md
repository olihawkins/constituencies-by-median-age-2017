# constituencies-by-median-age-2017

A D3 visualisation showing various results from the 2017 General Election broken down by median age in a grid histogram.

## Setup

Install Node modules with `npm install`.

Start bundling and transpiling with `npm run webpack`.

Run the local development version with `npm start`.

## Notes

Turn on source maps in webpack.config.js for development.
